/**
 * @author generated by eTrice
 *
 * Header File of ActorClass TrafficlightSensor
 *
 */

#ifndef _TEMPLATEMODEL_TRAFFICLIGHTSENSOR_H_
#define _TEMPLATEMODEL_TRAFFICLIGHTSENSOR_H_

#include "etDatatypes.h"
#include "messaging/etMessage.h"

/* include all referenced room classes */
#include "room/basic/service/timing/PTimer.h"
#include "TemplateModel/SendData.h"

/*--------------------- begin user code ---------------------*/
#include <stdlib.h>
#include <string.h>
#include <time.h>
#include <ctype.h>
struct timespec start,end;
/*--------------------- end user code ---------------------*/

typedef struct TrafficlightSensor TrafficlightSensor;

/* const part of ActorClass (ROM) */
typedef struct TrafficlightSensor_const {
	const char* instName;

	/* simple ports */
	const SendDataPort lightPort;
	const SendDataConjPort lightdata;

	/* data receive ports */

	/* saps */
	const PTimerConjPort timer;

	/* replicated ports */

	/* services */
} TrafficlightSensor_const;


/* constant for state machine data */
#define TRAFFICLIGHTSENSOR_HISTORY_SIZE 2

/* variable part of ActorClass (RAM) */
struct TrafficlightSensor {
	const TrafficlightSensor_const* const constData;

	/* data send ports */

	/*--------------------- attributes ---------------------*/
	 char str[40]
	;
	 char temp_string[40]
	;
	 float32 trafficLight;
	 char send_string[40]
	;


	/* state machine variables */
	etInt16 state;
	etInt16 history[TRAFFICLIGHTSENSOR_HISTORY_SIZE];
};

void TrafficlightSensor_init(TrafficlightSensor* self);

void TrafficlightSensor_receiveMessage(void* self, const void* ifitem, const etMessage* msg);


/*--------------------- user constructor/destructor ---------------------*/

/*--------------------- operations ---------------------*/


#endif /* _TEMPLATEMODEL_TRAFFICLIGHTSENSOR_H_ */

