/**
 * @author generated by eTrice
 *
 * Header File of ActorClass ValveActuator
 *
 */

#ifndef _TEMPLATEMODEL_VALVEACTUATOR_H_
#define _TEMPLATEMODEL_VALVEACTUATOR_H_

#include "etDatatypes.h"
#include "messaging/etMessage.h"

/* include all referenced room classes */
#include "room/basic/service/timing/PTimer.h"
#include "TemplateModel/SendData.h"

/*--------------------- begin user code ---------------------*/
#include <stdlib.h>
#include <string.h>
#include <time.h>
#include <ctype.h>
struct timespec valvestart,valveend;
/*--------------------- end user code ---------------------*/

typedef struct ValveActuator ValveActuator;

/* const part of ActorClass (ROM) */
typedef struct ValveActuator_const {
	const char* instName;

	/* simple ports */
	const SendDataPort receiveData;

	/* data receive ports */

	/* saps */
	const PTimerConjPort timer;

	/* replicated ports */

	/* services */
} ValveActuator_const;


/* constant for state machine data */
#define VALVEACTUATOR_HISTORY_SIZE 2

/* variable part of ActorClass (RAM) */
struct ValveActuator {
	const ValveActuator_const* const constData;

	/* data send ports */

	/*--------------------- attributes ---------------------*/
	 float64 wheelstart;
	 float32 abs;
	 float64 wheeltime;
	 float64 abstime;


	/* state machine variables */
	etInt16 state;
	etInt16 history[VALVEACTUATOR_HISTORY_SIZE];
};

void ValveActuator_init(ValveActuator* self);

void ValveActuator_receiveMessage(void* self, const void* ifitem, const etMessage* msg);


/*--------------------- user constructor/destructor ---------------------*/

/*--------------------- operations ---------------------*/
 bool ValveActuator_ALKchoicePoint0(ValveActuator* self, charPtr s_code);
 int32 ValveActuator_lane_change(ValveActuator* self);


#endif /* _TEMPLATEMODEL_VALVEACTUATOR_H_ */

