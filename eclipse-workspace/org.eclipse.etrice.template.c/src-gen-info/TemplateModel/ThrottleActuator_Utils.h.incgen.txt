/**
 * @author generated by eTrice
 *
 * Utils File of ActorClass ThrottleActuator
 *
 */

#ifndef _TEMPLATEMODEL_THROTTLEACTUATOR_UTILS_H_
#define _TEMPLATEMODEL_THROTTLEACTUATOR_UTILS_H_

#include "TemplateModel/ThrottleActuator.h"

/*
 * access macros for ports, operations and attributes
*/

/* simple event ports */
#define throttlePort_recursive2(data) SendDataPort_recursive2(&self->constData->throttlePort, data)
#define throttlePort_recvchangestate() SendDataPort_recvchangestate(&self->constData->throttlePort)

/* data receive ports */

/* data send ports */

/* saps */
#define timer_startTimer(data) PTimerConjPort_startTimer(&self->constData->timer, data)
#define timer_startTimeout(data) PTimerConjPort_startTimeout(&self->constData->timer, data)
#define timer_kill() PTimerConjPort_kill(&self->constData->timer)

/* replicated event ports */

/* services */

/* operations */

/* attributes */
#define recvscenarioID (self->recvscenarioID)
#define recvinstanceID (self->recvinstanceID)
#define recvemer (self->recvemer)
#define count (self->count)
#define recvairsystemstarttime (self->recvairsystemstarttime)
#define recvthrottlesensetime (self->recvthrottlesensetime)
#define recvfirsttime (self->recvfirsttime)
#define recvpedalsensetime (self->recvpedalsensetime)
#define recvpedalvotertime (self->recvpedalvotertime)
#define recvpedalfeeltime (self->recvpedalfeeltime)
#define recvthrottlecontrollertime (self->recvthrottlecontrollertime)
#define recvairsystemtime (self->recvairsystemtime)
#define recvairsystemtime2 (self->recvairsystemtime2)
#define thtacttime (self->thtacttime)
#define actor5_1_id (self->actor5_1_id)
#define actor5_2_id (self->actor5_2_id)
#define actor5_3_id (self->actor5_3_id)
#define actor5_4_id (self->actor5_4_id)
#define actor5_5_id (self->actor5_5_id)
#define actor5_6_id (self->actor5_6_id)
#define actor8_1_id (self->actor8_1_id)

/* port operations */

#endif /* _TEMPLATEMODEL_THROTTLEACTUATOR_UTILS_H_ */

