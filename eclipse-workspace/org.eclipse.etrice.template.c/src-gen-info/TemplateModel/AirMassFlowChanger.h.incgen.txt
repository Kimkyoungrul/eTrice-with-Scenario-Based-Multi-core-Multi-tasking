/**
 * @author generated by eTrice
 *
 * Header File of ActorClass AirMassFlowChanger
 *
 */

#ifndef _TEMPLATEMODEL_AIRMASSFLOWCHANGER_H_
#define _TEMPLATEMODEL_AIRMASSFLOWCHANGER_H_

#include "etDatatypes.h"
#include "messaging/etMessage.h"

/* include all referenced room classes */
#include "TemplateModel/SendData.h"

/*--------------------- begin user code ---------------------*/
#include <stdlib.h>
#include <string.h>
#include <time.h>
#include <ctype.h>
struct timespec airflowstart,airflowend;
/*--------------------- end user code ---------------------*/

typedef struct AirMassFlowChanger AirMassFlowChanger;

/* const part of ActorClass (ROM) */
typedef struct AirMassFlowChanger_const {
	const char* instName;

	/* simple ports */
	const SendDataPort airmassdata;
	const SendDataConjPort airmassdatasend;

	/* data receive ports */

	/* saps */

	/* replicated ports */

	/* services */
} AirMassFlowChanger_const;


/* constant for state machine data */
#define AIRMASSFLOWCHANGER_HISTORY_SIZE 2

/* variable part of ActorClass (RAM) */
struct AirMassFlowChanger {
	const AirMassFlowChanger_const* const constData;

	/* data send ports */

	/*--------------------- attributes ---------------------*/


	/* state machine variables */
	etInt16 state;
	etInt16 history[AIRMASSFLOWCHANGER_HISTORY_SIZE];
};

void AirMassFlowChanger_init(AirMassFlowChanger* self);

void AirMassFlowChanger_receiveMessage(void* self, const void* ifitem, const etMessage* msg);


/*--------------------- user constructor/destructor ---------------------*/

/*--------------------- operations ---------------------*/


#endif /* _TEMPLATEMODEL_AIRMASSFLOWCHANGER_H_ */

