/**
 * @author generated by eTrice
 *
 * Header File of ActorClass Receiver
 *
 */

#ifndef _TEMPLATEMODEL_RECEIVER_H_
#define _TEMPLATEMODEL_RECEIVER_H_

#include "etDatatypes.h"
#include "messaging/etMessage.h"

/* include all referenced room classes */
#include "room/basic/service/timing/PTimer.h"
#include "TemplateModel/PingPongProtocol.h"


typedef struct Receiver Receiver;

/* const part of ActorClass (ROM) */
typedef struct Receiver_const {
	const char* instName;

	/* simple ports */
	const PingPongProtocolPort recvPort;

	/* data receive ports */

	/* saps */
	const PTimerConjPort timingService;

	/* replicated ports */

	/* services */
} Receiver_const;


/* variable part of ActorClass (RAM) */
struct Receiver {
	const Receiver_const* const constData;

	/* data send ports */

	/*--------------------- attributes ---------------------*/

};

void Receiver_init(Receiver* self);

void Receiver_receiveMessage(void* self, const void* ifitem, const etMessage* msg);


/*--------------------- user constructor/destructor ---------------------*/

/*--------------------- operations ---------------------*/


#endif /* _TEMPLATEMODEL_RECEIVER_H_ */

