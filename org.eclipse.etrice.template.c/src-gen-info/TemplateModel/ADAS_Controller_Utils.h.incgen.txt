/**
 * @author generated by eTrice
 *
 * Utils File of ActorClass ADAS_Controller
 *
 */

#ifndef _TEMPLATEMODEL_ADAS_CONTROLLER_UTILS_H_
#define _TEMPLATEMODEL_ADAS_CONTROLLER_UTILS_H_

#include "TemplateModel/ADAS_Controller.h"

/*
 * access macros for ports, operations and attributes
*/

/* simple event ports */
#define relayPortA_sendData(data) SendDataConjPort_sendData(&self->constData->relayPortA, data)
#define relayPortB_sendData(data) SendDataConjPort_sendData(&self->constData->relayPortB, data)
#define relayPortC_sendData(data) SendDataConjPort_sendData(&self->constData->relayPortC, data)

/* data receive ports */

/* data send ports */

/* saps */

/* replicated event ports */
#define ifitem_index (((etReplSubPort*)ifitem)->index)
#define receiveData_recursive2_broadcast() SendDataReplPort_recursive2_broadcast(&self->constData->receiveData)
#define receiveData_recursive2(idx) SendDataReplPort_recursive2(&self->constData->receiveData, idx)

/* services */

/* operations */
#define ChoicePoint0(dist, velo, tl, cnum) ADAS_Controller_ChoicePoint0(self, dist, velo, tl, cnum)

/* attributes */
#define str (self->str)
#define retain_str (self->retain_str)
#define send_str (self->send_str)
#define temp_string (self->temp_string)
#define velocity_str (self->velocity_str)
#define distance_str (self->distance_str)
#define colliderNum_str (self->colliderNum_str)
#define trafficLight_str (self->trafficLight_str)
#define steering_str (self->steering_str)
#define velocity (self->velocity)
#define distance (self->distance)
#define colliderNum (self->colliderNum)
#define trafficLight (self->trafficLight)
#define steering (self->steering)
#define firstvelotime (self->firstvelotime)
#define firstcollidertime (self->firstcollidertime)
#define firststeertime (self->firststeertime)
#define firsttraffictime (self->firsttraffictime)
#define velorespon (self->velorespon)
#define colrespon (self->colrespon)
#define trafficrespon (self->trafficrespon)
#define steerrespon (self->steerrespon)

/* port operations */

#endif /* _TEMPLATEMODEL_ADAS_CONTROLLER_UTILS_H_ */

