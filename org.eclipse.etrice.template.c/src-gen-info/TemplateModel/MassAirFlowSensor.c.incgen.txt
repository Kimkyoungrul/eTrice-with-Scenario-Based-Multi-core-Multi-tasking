/**
 * @author generated by eTrice
 *
 * Source File of ActorClass MassAirFlowSensor
 *
 */

#include "MassAirFlowSensor.h"

#include "modelbase/etActor.h"
#include "debugging/etLogger.h"
#include "debugging/etMSCLogger.h"
#include "etUnit/etUnit.h"
#include "base/etMemory.h"

#include "room/basic/service/timing/PTimer.h"
#include "TemplateModel/SendData.h"

#include "MassAirFlowSensor_Utils.h"


/* interface item IDs */
enum interface_items {
	IFITEM_airflowdata = 1,
	IFITEM_airflowdata2 = 2,
	IFITEM_timer = 3
};

/* state IDs */
enum state_ids {
	NO_STATE = 0,
	STATE_TOP = 1,
	STATE_senseData = 2,
	STATE_MAX = 3
};

/* transition chains */
enum ChainIDs {
	CHAIN_TRANS_INITIAL_TO__senseData = 1,
	CHAIN_TRANS_tr0_FROM_senseData_TO_senseData_BY_timeouttimer_tr0 = 2
};

/* triggers */
enum triggers {
	POLLING = 0,
	TRIG_airflowdata__recursive2 = IFITEM_airflowdata + EVT_SHIFT*SendData_OUT_recursive2,
	TRIG_airflowdata__recvchangestate = IFITEM_airflowdata + EVT_SHIFT*SendData_OUT_recvchangestate,
	TRIG_airflowdata2__recursive2 = IFITEM_airflowdata2 + EVT_SHIFT*SendData_OUT_recursive2,
	TRIG_airflowdata2__recvchangestate = IFITEM_airflowdata2 + EVT_SHIFT*SendData_OUT_recvchangestate,
	TRIG_timer__timeout = IFITEM_timer + EVT_SHIFT*PTimer_OUT_timeout
};

/* state names */
static char* stateStrings[] = {"<no state>","<top>","senseData"
};

static void setState(MassAirFlowSensor* self, etInt16 new_state) {
	self->state = new_state;
	ET_MSC_LOGGER_CHANGE_STATE(self->constData->instName, stateStrings[new_state])
}

static etInt16 getState(MassAirFlowSensor* self) {
	return self->state;
}

/* Entry and Exit Codes */

/* Action Codes */
static void action_TRANS_INITIAL_TO__senseData(MassAirFlowSensor* self) {
    count=1;
    timer_startTimer(5);
}
static void action_TRANS_tr0_FROM_senseData_TO_senseData_BY_timeouttimer_tr0(MassAirFlowSensor* self, const InterfaceItemBase* ifitem) {
    ADASMessage ADASmessage;
    if(CHECKTIME==9||CHECKTIME==10||CHECKTIME==30||CHECKTIME==31){
    clock_gettime(CLOCK_MONOTONIC,&airflowstart);
    ADASmessage.actor_1 = ((1.0e+9*airflowstart.tv_sec)+airflowstart.tv_nsec)/1.0e+6;
    pthread_t id;
    id=pthread_self();
    ADASmessage.actor_1_id=(int)id;
    }
    for(int i =0;i<ONE_Milli_SEC_CONUT*0.172*MULCCS;i++){
    }
    ADASmessage.scenarioID = 3;
    ADASmessage.instanceID=count;
    //ADASmessage.prior=10;
    airflowdata_sendData(&ADASmessage);
    airflowdata2_sendData(&ADASmessage);
    count++;
    //if(count==10000)
    //timer.kill();
}

/* State Switch Methods */
/**
 * calls exit codes while exiting from the current state to one of its
 * parent states while remembering the history
 * @param current__et - the current state
 * @param to - the final parent state
 */
static void exitTo(MassAirFlowSensor* self, etInt16 current__et, etInt16 to) {
	while (current__et!=to) {
		switch (current__et) {
			case STATE_senseData:
				self->history[STATE_TOP] = STATE_senseData;
				current__et = STATE_TOP;
				break;
			default:
				/* should not occur */
				break;
		}
	}
}

/**
 * calls action, entry and exit codes along a transition chain. The generic data are cast to typed data
 * matching the trigger of this chain. The ID of the final state is returned
 * @param chain__et - the chain ID
 * @param generic_data__et - the generic data pointer
 * @return the +/- ID of the final state either with a positive sign, that indicates to execute the state's entry code, or a negative sign vice versa
 */
static etInt16 executeTransitionChain(MassAirFlowSensor* self, int chain__et, const InterfaceItemBase* ifitem, void* generic_data__et) {
	switch (chain__et) {
		case CHAIN_TRANS_INITIAL_TO__senseData:
		{
			action_TRANS_INITIAL_TO__senseData(self);
			return STATE_senseData;
		}
		case CHAIN_TRANS_tr0_FROM_senseData_TO_senseData_BY_timeouttimer_tr0:
		{
			action_TRANS_tr0_FROM_senseData_TO_senseData_BY_timeouttimer_tr0(self, ifitem);
			return STATE_senseData;
		}
			default:
				/* should not occur */
				break;
	}
	return NO_STATE;
}

/**
 * calls entry codes while entering a state's history. The ID of the final leaf state is returned
 * @param state__et - the state which is entered
 * @return - the ID of the final leaf state
 */
static etInt16 enterHistory(MassAirFlowSensor* self, etInt16 state__et) {
	if (state__et >= STATE_MAX) {
		state__et = (etInt16) (state__et - STATE_MAX);
	}
	while (ET_TRUE) {
		switch (state__et) {
			case STATE_senseData:
				/* in leaf state: return state id */
				return STATE_senseData;
			case STATE_TOP:
				state__et = self->history[STATE_TOP];
				break;
			default:
				/* should not occur */
				break;
		}
	}
	/* return NO_STATE; // required by CDT but detected as unreachable by JDT because of while (true) */
}

static void MassAirFlowSensor_executeInitTransition(MassAirFlowSensor* self) {
	int chain__et = CHAIN_TRANS_INITIAL_TO__senseData;
	etInt16 next__et = executeTransitionChain(self, chain__et, NULL, NULL);
	next__et = enterHistory(self, next__et);
	setState(self, next__et);
}

/* receiveEvent contains the main implementation of the FSM */
static void MassAirFlowSensor_receiveEventInternal(MassAirFlowSensor* self, InterfaceItemBase* ifitem, int localId, int evt, void* generic_data__et) {
	int trigger__et = localId + EVT_SHIFT*evt;
	int chain__et = NOT_CAUGHT;
	etInt16 catching_state__et = NO_STATE;
	((void)trigger__et);	/* avoids unused warning */

	if (!handleSystemEvent(ifitem, evt, generic_data__et)) {
		switch (getState(self)) {
		    case STATE_senseData:
		        switch(trigger__et) {
		                case TRIG_timer__timeout:
		                    {
		                        chain__et = CHAIN_TRANS_tr0_FROM_senseData_TO_senseData_BY_timeouttimer_tr0;
		                        catching_state__et = STATE_TOP;
		                    }
		                break;
		                default:
		                    /* should not occur */
		                    break;
		        }
		        break;
		    default:
		        /* should not occur */
		        break;
		}
	}
	if (chain__et != NOT_CAUGHT) {
		exitTo(self, getState(self), catching_state__et);
		{
			etInt16 next__et = executeTransitionChain(self, chain__et, ifitem, generic_data__et);
			next__et = enterHistory(self, next__et);
			setState(self, next__et);
		}
	}
}
static void MassAirFlowSensor_receiveEvent(MassAirFlowSensor* self, InterfaceItemBase* ifitem, int evt, void* generic_data__et) {
	int localId = (ifitem==NULL)? 0 : ifitem->localId;
	MassAirFlowSensor_receiveEventInternal(self, ifitem, localId, evt, generic_data__et);
}

void MassAirFlowSensor_init(MassAirFlowSensor* self){
	ET_MSC_LOGGER_SYNC_ENTRY("MassAirFlowSensor", "init")
	self->state = STATE_TOP;
	{
		int i;
		for (i=0; i<MASSAIRFLOWSENSOR_HISTORY_SIZE; ++i)
			self->history[i] = NO_STATE;
	}
	MassAirFlowSensor_executeInitTransition(self);
	ET_MSC_LOGGER_SYNC_EXIT
}


void MassAirFlowSensor_receiveMessage(void* self, const void* ifitem, const etMessage* msg){
	ET_MSC_LOGGER_SYNC_ENTRY("MassAirFlowSensor", "_receiveMessage")
	MassAirFlowSensor_receiveEvent(self, (etPort*)ifitem, msg->evtID, (void*)(((char*)msg)+MEM_CEIL(sizeof(etMessage))));

	ET_MSC_LOGGER_SYNC_EXIT
}


/*--------------------- user constructor/destructor ---------------------*/

/*--------------------- operations ---------------------*/

