/**
 * @author generated by eTrice
 *
 * Header File of ActorClass pedalfeel
 *
 */

#ifndef _TEMPLATEMODEL_PEDALFEEL_H_
#define _TEMPLATEMODEL_PEDALFEEL_H_

#include "etDatatypes.h"
#include "messaging/etMessage.h"

/* include all referenced room classes */
#include "TemplateModel/SendData.h"

/*--------------------- begin user code ---------------------*/
#include <stdlib.h>
#include <string.h>
#include <time.h>
#include <ctype.h>
struct timespec feelstart,feelend;
/*--------------------- end user code ---------------------*/

typedef struct pedalfeel pedalfeel;

/* const part of ActorClass (ROM) */
typedef struct pedalfeel_const {
	const char* instName;

	/* simple ports */
	const SendDataPort feeldata;
	const SendDataConjPort feeldatasend;

	/* data receive ports */

	/* saps */

	/* replicated ports */

	/* services */
} pedalfeel_const;


/* constant for state machine data */
#define PEDALFEEL_HISTORY_SIZE 2

/* variable part of ActorClass (RAM) */
struct pedalfeel {
	const pedalfeel_const* const constData;

	/* data send ports */

	/*--------------------- attributes ---------------------*/
	 float64 recvthrottlesensetime;
	 float64 recvfirsttime;
	 float64 recvpedalsensetime;
	 float64 recvairsystemstarttime;


	/* state machine variables */
	etInt16 state;
	etInt16 history[PEDALFEEL_HISTORY_SIZE];
};

void pedalfeel_init(pedalfeel* self);

void pedalfeel_receiveMessage(void* self, const void* ifitem, const etMessage* msg);


/*--------------------- user constructor/destructor ---------------------*/

/*--------------------- operations ---------------------*/


#endif /* _TEMPLATEMODEL_PEDALFEEL_H_ */

