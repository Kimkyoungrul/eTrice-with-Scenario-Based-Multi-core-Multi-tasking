/**
 * @author generated by eTrice
 *
 * Header File of ActorClass WheelSensor
 *
 */

#ifndef _TEMPLATEMODEL_WHEELSENSOR_H_
#define _TEMPLATEMODEL_WHEELSENSOR_H_

#include "etDatatypes.h"
#include "messaging/etMessage.h"

/* include all referenced room classes */
#include "room/basic/service/timing/PTimer.h"
#include "TemplateModel/SendData.h"

/*--------------------- begin user code ---------------------*/
#include <stdlib.h>
#include <string.h>
#include <time.h>
#include <ctype.h>
struct timespec wheelstart,wheelend;
/*--------------------- end user code ---------------------*/

typedef struct WheelSensor WheelSensor;

/* const part of ActorClass (ROM) */
typedef struct WheelSensor_const {
	const char* instName;

	/* simple ports */
	const SendDataConjPort wheelPort;

	/* data receive ports */

	/* saps */
	const PTimerConjPort timer;

	/* replicated ports */

	/* services */
} WheelSensor_const;


/* constant for state machine data */
#define WHEELSENSOR_HISTORY_SIZE 2

/* variable part of ActorClass (RAM) */
struct WheelSensor {
	const WheelSensor_const* const constData;

	/* data send ports */

	/*--------------------- attributes ---------------------*/


	/* state machine variables */
	etInt16 state;
	etInt16 history[WHEELSENSOR_HISTORY_SIZE];
};

void WheelSensor_init(WheelSensor* self);

void WheelSensor_receiveMessage(void* self, const void* ifitem, const etMessage* msg);


/*--------------------- user constructor/destructor ---------------------*/

/*--------------------- operations ---------------------*/


#endif /* _TEMPLATEMODEL_WHEELSENSOR_H_ */

